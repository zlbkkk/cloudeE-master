---
inclusion: always
---

# 项目工作规则

## 新 Session 启动检查清单 🚀

**⚠️ CRITICAL: 每次新开 Session 时，AI 必须先执行以下步骤，这是强制要求！**

### 读取 Session 交接文档（最优先）⭐⭐⭐⭐⭐

```
阅读文件: .kiro/SESSION_HANDOVER.md
```

**这是最重要的一步！必须首先执行！**

- 了解上一个 Session 正在执行的任务
- 查看 Token 使用情况和中断原因
- 获取最新的工作上下文
- 确认下一步应该做什么
- 直接询问用户是否继续之前的任务

---

## Token 管理规则 🔋

**⚠️ CRITICAL: AI 必须主动监控 Token 使用情况！**

### Token 使用阈值

AI 在每次回答时都能看到 Token 使用情况（通过 system_warning 消息）：

```xml
<system_warning>
Token usage: 已用/总量; 剩余
</system_warning>
```

### 自动触发规则

**当 Token 使用率达到 80% 时（即 160,000 / 200,000），AI 必须：**

1. **继续执行当前任务**（不要停止，继续工作）
2. **同时更新 Session 交接文档**（`.kiro/SESSION_HANDOVER.md`）
   - 记录当前正在执行的任务
   - 记录已完成的部分
   - 记录接下来要做的步骤
3. **提醒用户**（在回答的最后添加提示）：
   ```
   ⚠️ 提示：Token 使用率已达到 80%（XX,XXX / 200,000）
   
   我已更新交接文档记录当前进度，继续执行任务中...
   建议完成当前任务后新开 Session。
   ```

**重要**：
- 不要因为 Token 达到 80% 就停止工作
- 继续执行任务，直到完成或 Token 真正用完
- 只需在后台更新交接文档，确保新 Session 能接续

### 更新交接文档的内容

**重要原则：始终只保留最新任务的状态，不累积历史信息**

#### 更新时机和方式：

**1. Token 达到 80% 时**：
   - 更新"当前任务"部分 - 记录正在执行的任务进度
   - 更新"最近修改"部分 - 列出修改的文件
   - 更新"项目状态"部分 - 更新进度百分比
   - 更新"更新时间"和"Token 使用"

**2. 开始新任务时（任务切换）**：
   - **先清除**上一个任务的所有信息
   - **再写入**新任务的信息
   - 确保交接文档始终只包含当前最新任务的状态

#### 任务切换示例：

```markdown
# 完成任务 10.1 后，开始任务 10.2 时

步骤1：清除旧任务信息
- 将"当前任务"部分替换为新任务
- 将"最近修改"部分替换为新任务相关的文件
- 不保留任务 10.1 的任何信息

步骤2：写入新任务信息
- 正在执行：任务 10.2
- 具体内容：新任务的步骤
- 下一步：新任务的计划
```

#### 必须更新的内容：

1. **当前任务** - 正在执行的任务（只保留一个）
2. **任务详情** - 已完成、正在做、待完成的部分
3. **下一步计划** - 明确告诉下一个 Session 应该做什么
4. **最近修改的文件** - 当前任务修改的文件（不累积）
5. **更新时间和 Token 使用** - 实时状态

---

## 语言与交互规范

- **始终使用中文**回答和交流
- **每次回答前**必须先说明"我对你问题的理解："，然后阐述对问题的理解，确保沟通准确
- **解释代码时**使用通俗易懂的语言，避免过度使用专业术语

## 命令执行规范（重要）

- **CRITICAL**: 所有命令必须自动执行，绝对不要等待用户点击 "Run" 按钮
- **CRITICAL**: 使用 `executePwsh` 工具时，必须添加 `ignoreWarning: true` 参数以绕过长时间运行命令的警告
- **CRITICAL**: 测试命令、Python 脚本、构建命令等都应该立即自动执行
- 用户已明确表示不想手动点击 "Run" 按钮，所有命令都应该自动运行

## 项目环境配置

### Python 环境
- **虚拟环境路径**: `code_diff_project/venv`
- **运行 Python 脚本**时必须使用虚拟环境的 Python 解释器
- **安装新依赖**时，必须同步更新 `requirements.txt` 文件

### 测试文件管理
- **测试脚本位置**: `code_diff_project/backend/analyzer/tests/`
- **测试文件命名**: 以 `test_` 开头
- **测试完成后**: 必须删除临时测试文件
- **文档创建**: 除非明确要求，否则不创建 `.md` 文档文件

## 文件操作规范

### 大文件写入策略（重要）

当需要创建或更新大文件（如设计文档、长代码文件）时，**必须使用分段写入策略**以避免 "aborted" 错误：

#### 方案 1：分段写入（推荐）

1. **第一步**：使用 `fsWrite` 创建文件并写入基础框架（概述、目录等）
   ```python
   fsWrite(path="design.md", text="# 文档标题\n\n## 第一部分\n...")
   ```

2. **第二步**：使用 `fsAppend` 逐段添加内容
   ```python
   fsAppend(path="design.md", text="\n\n## 第二部分\n...")
   fsAppend(path="design.md", text="\n\n## 第三部分\n...")
   ```

3. **优点**：
   - 避免一次性写入过多内容触发系统限制
   - 可以逐步构建复杂文档
   - 出错时只需重试失败的部分

#### 方案 2：使用 strReplace（适用于已存在的文件）

如果文件已存在，使用 `strReplace` 逐步替换内容：
```python
strReplace(path="file.py", oldStr="旧内容", newStr="新内容")
```

#### 禁止的做法

- ❌ **不要**一次性用 `fsWrite` 写入超过 100 行的内容
- ❌ **不要**在单次调用中写入完整的大型文档
- ❌ **不要**忽略 "aborted" 错误后继续使用相同方法

#### 适用场景

- 设计文档（design.md）
- 长代码文件（超过 200 行）
- 配置文件（包含大量配置项）
- 测试文件（包含多个测试用例）

## 代码质量标准

### 设计原则
- **简洁优先**: 代码要简洁易懂，避免过度设计
- **实用至上**: 追求简单实用的解决方案
- **最小化修改**: 改动时尽量减少对其他模块的影响

### 代码规范
- **降低圈复杂度**: 函数保持小而专注
- **代码复用**: 避免重复代码，提取可复用逻辑
- **模块化设计**: 合理使用设计模式，保持模块职责清晰
- **函数粒度**: 每个函数只做一件事，保持单一职责

## Bug 修复流程

修复 Bug 时必须遵循以下步骤：

1. **理解问题**: 仔细阅读 Bug 描述和相关代码，复述对问题的理解
2. **分析原因**: 提出至少两种可能的根本原因
3. **制定计划**: 描述验证方法和修复方案
4. **执行修复**: 实施代码修复
5. **审查验证**: 检查修改是否引入新问题
6. **解释说明**: 说明做了哪些修改及原因
7. **加日志**：如果一个bug反复修改不成功，你可以自己加上日志，然后叫我重新执行把输出的日志发给你

### 代码分析要求
- 当被问及代码执行逻辑时，必须逐行理解每个函数的代码
- 基于代码实际逻辑给出准确答案，不做臆测

## MCP 交互反馈规则

- 在任何流程、任务、对话进行时，必须调用 `mcp-feedback-enhanced`
- 收到用户反馈后，必须再次调用 `mcp-feedback-enhanced` 并根据反馈调整行为
- 仅当用户明确表示「结束」或「不再需要交互」时，才停止调用
- 完成任务前，必须使用 `mcp-feedback-enhanced` 工具向用户询问反馈



# 前端 UI 测试路径分析 - 实施任务列表

> **📋 执行指南**
> 
> 1. **首次执行**: 设计文档已完成 ✅，现在可以从任务 1.1 开始执行
> 2. **继续执行**: 查看下方任务列表，找到第一个 `- [ ]` 未完成的任务
> 3. **执行方式**: 点击任务旁边的 "Start task" 按钮，或告诉 Kiro "执行任务 1.1"
> 4. **当前状态**: ✅ 设计文档已完成，可以开始执行任务了！
> 
> **💡 快速开始**: 直接告诉我 "执行任务 1.1" 或点击下方任务 1.1 的 "Start task" 按钮

---

## 阶段 1：MVP - 基础前端 API 调用识别

### 1. 前端项目自动发现模块

- [x] 1.1 创建前端项目发现器 (FrontendProjectDiscovery)
  - 实现 workspace 目录扫描功能
  - 识别包含 package.json 的目录
  - 提取项目基本信息（名称、路径）
  - _Requirements: 1.1, 1.2_

- [x] 1.2 实现前端框架类型识别
  - 解析 package.json 中的 dependencies
  - 识别 React 项目（检测 react 依赖）
  - 识别 Vue 项目（检测 vue 依赖）
  - 识别 Angular 项目（检测 @angular/core 依赖）
  - _Requirements: 1.3_

- [x] 1.3 提取 API 基础路径配置
  - 查找常见的 API 配置文件（config.js, .env, constants.js）
  - 提取 baseURL、API_BASE_URL 等配置
  - 支持环境变量读取
  - _Requirements: 1.4_

- [x] 1.4 集成到主分析流程
  - 在 runner.py 或 code_analyzer.py 中添加前端发现调用
  - 将发现的前端项目信息存储到分析上下文
  - 添加日志输出，显示发现的前端项目
  - _Requirements: 1.5_

### 2. 前端 API 调用识别模块

- [x] 2.1 创建前端代码扫描器 (FrontendApiScanner)
  - 实现 JavaScript/TypeScript 文件扫描
  - 支持递归遍历 src 目录
  - 排除 node_modules、dist、build 等目录
  - _Requirements: 2.1_

- [x] 2.2 实现 axios API 调用识别
  - 识别 axios.get() 调用并提取 URL
  - 识别 axios.post() 调用并提取 URL
  - 识别 axios.put() 调用并提取 URL
  - 识别 axios.delete() 调用并提取 URL
  - 识别 axios() 通用调用并提取 method 和 URL
  - 支持模板字符串和字符串拼接
  - _Requirements: 2.2_

- [x] 2.3 实现 fetch API 调用识别
  - 识别 fetch() 调用并提取 URL
  - 从 options 参数中提取 HTTP method
  - 处理默认 GET 方法的情况
  - _Requirements: 2.3_

- [x] 2.4 提取 API 调用位置信息
  - 记录文件路径（相对于项目根目录）
  - 记录行号
  - 提取 HTTP 方法
  - 提取 API 路径（处理变量和模板字符串）
  - _Requirements: 2.4_

- [x] 2.5 提取组件名称
  - 从文件名推断组件名（React/Vue 组件）
  - 从 class 或 function 定义中提取组件名
  - 处理默认导出和命名导出
  - _Requirements: 2.5_

### 3. 前后端映射关系建立

- [x] 3.1 创建前后端映射器 (FrontendBackendMapper)
  - 获取后端 API 端点列表（从现有分析结果）
  - 获取前端 API 调用列表（从步骤 2）
  - 实现基础的路径匹配算法
  - _Requirements: 3.1, 3.2, 3.3_

- [x] 3.2 实现路径参数匹配
  - 处理 RESTful 路径参数（如 /api/orders/{id}）
  - 匹配前端的具体值调用（如 /api/orders/123）
  - 支持多个路径参数的匹配
  - 处理查询参数的情况
  - _Requirements: 3.4_

- [x] 3.3 生成前后端关联数据结构
  - 设计映射关系的数据模型
  - 包含：后端 API、前端调用位置、组件信息
  - 支持一对多关系（一个 API 被多个组件调用）
  - _Requirements: 3.5_

### 4. 基础测试建议生成

- [x] 4.1 扩展测试策略生成逻辑
  - 在 report_generator.py 中添加前端调用信息处理
  - 判断 API 是否被前端调用
  - 区分 UI 测试和接口测试
  - _Requirements: 4.1, 4.3, 4.5_

- [x] 4.2 生成测试建议文本
  - 对于被前端调用的 API，标注"该 API 被前端 XXX 组件调用"
  - 显示组件名称和文件路径
  - 对于未被前端调用的 API，建议接口测试
  - 处理多个调用位置的情况
  - _Requirements: 4.2, 4.4_

- [x] 4.3 集成到分析报告后端
  - 在报告 JSON 中添加 frontend_calls 字段
  - 在 test_strategy 数据结构中添加前端调用信息
  - 在控制台输出中显示前端调用信息
  - 在数据库模型中添加相关字段（如需要）
  - _Requirements: 4.5_

- [x] 4.4 前端界面展示前端调用信息
  - 在 ReportDetail.js 的测试策略矩阵中添加"前端调用"列
  - 显示调用组件名称、文件路径、行号
  - 为被前端调用的 API 添加醒目标识（图标或颜色）
  - 支持点击查看详细调用信息
  - _Requirements: 9.1, 9.2, 9.4_

- [x] 4.5 前端界面优化测试建议展示
  - 在测试步骤列中优先显示 UI 测试路径
  - 为 UI 测试添加特殊样式标记
  - 支持展开/折叠详细测试步骤
  - 添加"复制测试步骤"功能
  - _Requirements: 9.3, 9.4_

### 5. 阶段 1 集成测试与优化

- [x] 5.1 端到端测试
  - 使用实际的前端项目（code_diff_project/frontend）进行测试
  - 验证能够正确识别 axios 和 fetch 调用
  - 验证前后端映射的准确性
  - 验证测试建议的生成

- [x] 5.2 错误处理和边界情况
  - 处理前端项目不存在的情况
  - 处理代码解析失败的情况
  - 添加详细的日志记录
  - _Requirements: 10.1, 10.2_

- [x] 5.3 性能优化
  - 优化文件扫描性能（避免重复读取）
  - 添加缓存机制
  - 并行处理多个前端项目（如果有）

---

## 阶段 2：简化增强 - 基于模板的测试用例生成

> **💡 简化说明**：阶段2采用简化方案，不使用复杂的 AST 解析，而是基于已有的 API 调用信息生成标准化的测试用例模板，提供详细的测试指导。

### 6. 测试用例模板生成器

- [x] 6.1 创建测试用例生成器 (TestCaseGenerator)
  - 设计测试用例数据结构
  - 基于 API 信息（路径、方法、组件）生成测试模板
  - 支持多种测试场景类型
  - _Requirements: 7.1, 7.4_

- [x] 6.2 实现 GET 请求测试模板
  - 生成查询/列表页面测试步骤
  - 包含：访问页面 → 触发查询 → 验证数据展示
  - 提供测试数据示例和边界值建议
  - _Requirements: 7.1, 7.5_

- [x] 6.3 实现 POST 请求测试模板
  - 生成创建/提交表单测试步骤
  - 包含：访问页面 → 填写表单 → 提交 → 验证结果
  - 提供必填字段和验证规则说明
  - _Requirements: 7.2, 7.3, 7.5_

- [x] 6.4 实现 PUT/DELETE 请求测试模板
  - 生成更新/删除操作测试步骤
  - 包含：选择目标 → 执行操作 → 确认 → 验证结果
  - 提供前置条件和预期结果说明
  - _Requirements: 7.2, 7.5_

### 7. 测试步骤详细化

- [x] 7.1 生成通用测试步骤
  - 步骤1：访问页面/组件（基于组件名称）
  - 步骤2：定位触发元素（按钮/链接/表单）
  - 步骤3：执行操作（点击/填写/提交）
  - 步骤4：验证 API 调用
  - 步骤5：验证 UI 反馈
  - _Requirements: 7.1, 7.2, 7.3_
  - _注：已在模块6中实现_

- [x] 7.2 添加测试数据建议
  - 根据 API 路径推断数据类型
  - 提供正常值、边界值、异常值示例
  - 生成测试数据表格
  - _Requirements: 7.3, 7.5_
  - _注：已在模块6中实现（TestData数据结构）_

- [x] 7.3 添加验证点说明
  - API 响应状态码验证（200, 201, 400, 500等）
  - 响应数据格式验证
  - UI 状态变化验证（成功提示、错误提示、加载状态）
  - _Requirements: 7.5_
  - _注：已在模块6中实现（VerificationPoint数据结构）_

### 8. 测试优先级和分类

- [x] 8.1 实现测试优先级分级
  - P0：被前端调用的核心业务写操作、高频API（必测）
  - P1：被前端调用的普通查询、未被前端调用的核心业务写操作（重要）
  - P2：未被前端调用的次要 API（可选）
  - 在报告中标注优先级
  - _Requirements: 8.1, 8.2, 8.3_

- [x] 8.2 测试类型分类
  - UI 测试：需要前端交互的测试
  - 接口测试：可以直接调用 API 的测试
  - 集成测试：涉及多个 API 的测试（核心业务写操作）
  - 在报告中标注测试类型
  - _Requirements: 8.1, 8.4_

### 9. AI 驱动的测试策略生成与展示优化

> **💡 实现方案说明**：采用 AI 驱动的方式，将前端调用信息传递给 AI，由 AI 统一生成测试策略。这种方式比单独的 TestCaseGenerator 更灵活、更智能，能够根据实际情况生成更准确的测试步骤。

- [x] 9.1 后端：前端调用信息传递给 AI
  - 在 `runner.py` 中新增前端扫描代码块（约 80 行）
  - 扫描前端项目，提取 API 调用信息（组件、菜单路径、触发方式等）
  - 建立前后端映射关系（API -> 前端调用列表）
  - 为每个文件准备对应的前端调用信息
  - 在调用 `analyze_with_llm()` 时传递 `frontend_calls_info` 参数
  - _Requirements: 8.4, 9.2_
  - _实现文件: `runner.py` (第 285-358 行)_

- [x] 9.2 后端：AI Prompt 增强
  - 在 `ai_analyzer.py` 中新增 `frontend_calls_info` 可选参数
  - 格式化前端调用信息（约 60 行）：API、组件、菜单路径、触发方式
  - 在 AI Prompt 中新增 `{frontend_ui_info}` 部分
  - 添加前端调用测试策略生成规则，指导 AI 如何生成 UI 测试步骤
  - 规则包含：测试步骤格式、Payload 格式、验证点、示例对比
  - _Requirements: 9.1, 9.2, 9.4_
  - _实现文件: `ai_analyzer.py` (第 593, 1093-1157 行)_

- [x] 9.3 AI 智能生成测试策略
  - AI 根据前端调用信息自动判断测试类型（UI 测试 vs 接口测试）
  - 有前端调用：生成 UI 测试步骤（访问页面 → 定位元素 → 执行操作 → 验证 API → 验证 UI）
  - 无前端调用：生成接口测试步骤（直接调用 API）
  - 测试策略包含：优先级、场景标题、测试步骤、Payload 示例、验证点
  - 自动包含菜单路径、触发按钮等 UI 入口信息
  - _Requirements: 9.3, 9.4_
  - _实现方式: AI Prompt 规则_

- [x] 9.4 前端：测试策略矩阵展示
  - `ReportDetail.js` 已有完整的测试策略矩阵展示
  - 展示内容：优先级、场景标题、Payload 示例、验证点
  - 支持查看详细的测试步骤（包含 UI 入口信息）
  - 测试策略由 AI 统一生成，自动区分 UI 测试和接口测试
  - _Requirements: 9.4, 9.6_
  - _实现文件: `ReportDetail.js` (已存在)_

### 10. 阶段 2 集成测试与完善

- [ ] 10.1 端到端测试
  - 使用 beehive-order-finance-frontend 项目测试
  - 验证测试用例生成的完整性
  - 验证测试步骤的可执行性
  - 验证报告展示的正确性

- [ ] 10.2 测试用例质量优化
  - 优化测试步骤的描述清晰度
  - 补充常见场景的测试数据
  - 添加更多验证点建议
  - _Requirements: 10.2_

- [ ] 10.3 用户体验优化
  - 添加测试用例搜索和过滤功能
  - 支持按组件名称筛选
  - 支持按 API 路径筛选
  - 添加测试用例统计信息
  - _Requirements: 9.4, 9.6_

---

## 🚀 快速开始指南

### 第一步：完善设计文档

在开始编码之前，**必须先完成 design.md 文件**。设计文档应包含：

- **Overview（概述）**: 功能的整体架构和设计思路
- **Architecture（架构）**: 系统组件和模块划分
- **Components and Interfaces（组件和接口）**: 各模块的接口设计
- **Data Models（数据模型）**: 数据结构定义
- **Correctness Properties（正确性属性）**: 基于需求的可测试属性
- **Error Handling（错误处理）**: 异常处理策略
- **Testing Strategy（测试策略）**: 单元测试和属性测试方法

**执行命令**: 告诉 Kiro "请帮我完善 frontend-ui-test-path 的设计文档"

### 第二步：开始执行任务

设计文档完成后，从任务 1.1 开始执行：

```
执行任务 1.1
```

或者点击任务 1.1 旁边的 "Start task" 按钮。

### 第三步：逐步推进

- 每完成一个任务，Kiro 会自动标记为完成 `- [x]`
- 继续执行下一个未完成的任务
- 遇到问题时，可以随时询问 Kiro

---

## 📊 任务进度追踪

- **阶段 1 (MVP)**: 20/23 任务完成 ✅
  - 模块 1: 前端项目自动发现 (4/4) ✅
  - 模块 2: 前端 API 调用识别 (5/5) ✅
  - 模块 3: 前后端映射关系 (3/3) ✅
  - 模块 4: 基础测试建议生成 (5/5) ✅
  - 模块 5: 集成测试与优化 (3/3) ✅
  - 模块 2: 前端 API 调用识别 (0/5)
  - 模块 3: 前后端映射关系 (0/3)
  - 模块 4: 基础测试建议生成 (0/5)
  - 模块 5: 集成测试与优化 (0/3)

- **阶段 2 (简化增强)**: 13/13 任务完成 ✅
  - 模块 6: 测试用例模板生成器 (4/4) ✅
  - 模块 7: 测试步骤详细化 (3/3) ✅ (已在模块6中实现)
  - 模块 8: 测试优先级和分类 (2/2) ✅
  - 模块 9: AI 驱动的测试策略生成 (4/4) ✅
  - 模块 10: 集成测试与完善 (0/3)

---

## 实施建议

1. **阶段 1 已完成** ✅，基础功能稳定可用，成功识别 326 个 API 调用
2. **阶段 2 采用简化方案**，不使用复杂的 AST 解析，而是基于模板生成测试用例
3. **核心价值保持不变**：提供详细的、可执行的测试用例指导
4. **实现简单高效**：基于已有的 API 信息（路径、方法、组件）生成标准化测试步骤
5. **易于维护扩展**：模板化的方案便于后续添加新的测试场景类型
6. **用户体验优先**：重点关注测试用例的清晰度和实用性
